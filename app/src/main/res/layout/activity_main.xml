<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <Button
        android:id="@+id/giveInfo"
        android:layout_width="350dp"
        android:layout_height="83dp"
        android:layout_marginTop="388dp"
        android:onClick="btnClicked"
        android:text="Give Information about poor family"
        android:textSize="18sp"
        app:layout_constraintStart_toStartOf="@+id/giveInfo"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/giveInfo"
        android:layout_width="349dp"
        android:layout_height="80dp"
        android:layout_marginStart="16dp"
        android:layout_marginLeft="16dp"
        android:onClick="btnClicked"
        android:text="GET INFORMATION ABOUT POOR FAMILIES"
        android:textSize="18sp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/giveInfo" />
</androidx.constraintlayout.widget.ConstraintLayout>